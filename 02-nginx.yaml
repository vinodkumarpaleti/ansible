- name: Installing nginx in host  #name is not mandatory, this is best practice
  host: mongodb
  become: yes #This will give root access
  tasks:
  - name: Install Nginx
    ansible.builtin.yum:
      name: nginx
      state: Installed
  - name: Start Nginx
    ansible.builtin.service:
      name: nginx
      state: started